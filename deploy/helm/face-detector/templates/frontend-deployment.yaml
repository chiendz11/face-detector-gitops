apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.frontendAdmin.service.name }}
  labels:
    app.kubernetes.io/name: face-detector
    app.kubernetes.io/component: frontend-admin
spec:
  replicas: {{ .Values.frontendAdmin.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: face-detector
      app.kubernetes.io/component: frontend-admin
  template:
    metadata:
      labels:
        app.kubernetes.io/name: face-detector
        app.kubernetes.io/component: frontend-admin
    spec:
{{- with .Values.imagePullSecrets }}
      imagePullSecrets:
{{ toYaml . | indent 8 }}
{{- end }}
      securityContext:
        runAsNonRoot: true
        runAsUser: 101
        runAsGroup: 101
        fsGroup: 101
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: frontend-admin
          image: "{{ include "face-detector.imageRef" .Values.frontendAdmin.image }}"
          imagePullPolicy: {{ .Values.frontendAdmin.image.pullPolicy }}
          securityContext:
            allowPrivilegeEscalation: false
          ports:
            - containerPort: {{ .Values.frontendAdmin.service.containerPort }}
          readinessProbe:
            httpGet:
              path: /
              port: {{ .Values.frontendAdmin.service.containerPort }}
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: {{ .Values.frontendAdmin.service.containerPort }}
            initialDelaySeconds: 15
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.frontendAdmin.service.name }}
  labels:
    app.kubernetes.io/name: face-detector
    app.kubernetes.io/component: frontend-admin
spec:
  selector:
    app.kubernetes.io/name: face-detector
    app.kubernetes.io/component: frontend-admin
  ports:
    - name: http
      port: {{ .Values.frontendAdmin.service.port }}
      targetPort: {{ .Values.frontendAdmin.service.containerPort }}
  type: ClusterIP
