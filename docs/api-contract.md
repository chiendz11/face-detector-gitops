# API Contract

The source-of-truth API contract now lives in [docs/api-contract.yml](docs/api-contract.yml).

This Markdown file is kept only as a pointer so the backend, frontend, and integration smoke tests do not drift across two separate contract documents.